/**
 * Base type for data passed to the host component via the `HOST_DATA` token.
 * Accepts any object shape — use a more specific interface in your own host components.
 */
export type IHostData = object;
