/**
 * Base type for data passed to the dialog content via the `DIALOG_DATA` token.
 * Accepts any object shape — use a more specific interface in your own components.
 */
export type IDialogData = object;
