import { Type } from "@angular/core";

/**
 * Represents an Angular component class that can be dynamically instantiated.
 * Alias for `Type<any>` used throughout the dialog configuration.
 */
export interface Component extends Type<any> {}
